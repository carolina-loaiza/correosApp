//hay que jalar:

//clientes/paquetes

/*
hora y fecha a la hora de cambiar estados (se jala de la 
las vistas de e.aduanas, e.sucursal, y e.repartidor)
 */

//el usuario que cambió el estado

/*Hay que hacer una funcion que en el momento que se cambie el estado,
se guarde el usuario loggeado.*/

let listaPaquetes = obtenerDatoLocal('paquetesLS');

mostrarBitacora();


function mostrarBitacora() {
    
}